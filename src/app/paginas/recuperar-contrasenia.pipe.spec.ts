import { RecuperarContraseniaPipe } from './recuperar-contrasenia.pipe';

describe('RecuperarContraseniaPipe', () => {
  it('create an instance', () => {
    const pipe = new RecuperarContraseniaPipe();
    expect(pipe).toBeTruthy();
  });
});
