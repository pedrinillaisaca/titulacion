import { FiltroCoincidentPipe } from './filtro-coincident.pipe';

describe('FiltroCoincidentPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCoincidentPipe();
    expect(pipe).toBeTruthy();
  });
});
