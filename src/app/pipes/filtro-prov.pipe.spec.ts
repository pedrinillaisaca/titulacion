import { FiltroProvPipe } from './filtro-prov.pipe';

describe('FiltroProvPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProvPipe();
    expect(pipe).toBeTruthy();
  });
});
